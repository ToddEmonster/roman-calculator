// Real calculator
function sum(first, second) {
    return first + second;
}
// HTML display
// window.onload = function() {
//     // document.body.textContent = greeter(user);
// }
module.exports = sum;
